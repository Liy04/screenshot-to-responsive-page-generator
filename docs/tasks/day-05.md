# Week 10 Day 05

## 线程

前端线程

## 任务目标

清晰展示 `REAL_AI` / `FALLBACK` / `FAILED` / `TIMEOUT` 状态，并稳定保持 iframe preview 安全。

## 必须完成

1. 页面展示 `fallbackReason`、`warnings`、`errors`。
2. 页面展示状态来源和关键信息。
3. 保持 iframe preview 稳定。
4. iframe 继续使用 `sandbox=""`，且无 `allow-scripts`。

## 禁止事项

- 不做复杂 UI 框架迁移。
- 不做编辑器。

## 验收标准

- 前端 build 通过。
- 前端测试覆盖状态展示与 iframe 安全。
